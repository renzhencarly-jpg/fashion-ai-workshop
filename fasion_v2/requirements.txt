# ============================================================================
# Shopping Guide AI (Fashion v2) - pinned dependencies
# Tested with Python 3.11.
# Search-and-checkout only: no Gemini, no Azure Blob, no image/video pipeline.
# ============================================================================

# --- Utilities ---
python-dotenv>=1.2.1,<2.0
certifi>=2026.1.4
requests>=2.32.5,<3.0

# --- MongoDB ---
pymongo>=4.15.5,<5.0

# --- LangChain / LangGraph ---
langgraph>=1.0.6,<2.0
langgraph-checkpoint-mongodb>=0.3.0,<0.4
langchain-core>=1.2.7,<2.0
langchain-openai>=1.1.7,<2.0

# --- LLM / AI providers ---
openai>=2.15.0,<3.0          # used by Mem0's azure_openai provider
voyageai>=0.3.7,<1.0
mem0ai>=2.0.1,<3.0
azure-identity>=1.25.0,<2.0  # required by Mem0's azure_openai embedder
azure-core>=1.40.0,<2.0      # dependency of azure-identity

# --- Web UI / server ---
gradio>=6.3.0,<7.0
fastapi>=0.128.0,<1.0
uvicorn>=0.40.0,<1.0
